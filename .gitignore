.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
node_modules/
frontend/dist/
build/
outputs/
multirun/
lightning_logs/
checkpoints/
data/cache/
figures/

# NOTE: frontend/public/operator/{manifest.json,weights.bin} are committed on purpose so
# the demo works from a fresh clone and GitHub Pages has something to deploy. Exported at
# half precision (~2.4 MiB), which costs 2.4% of the model's own error. Regenerate with:
#   uv run qft-operator-export --checkpoint <run>/checkpoints/best.ckpt \
#       --dtype float16 --output frontend/public/operator
.idea/
.DS_Store
