max_epochs: 200
accelerator: auto
devices: auto
strategy: auto
precision: 32-true
# The scaling and RG terms differentiate the network, so validation cannot run under
# inference mode -- its tensors are excluded from autograd.
inference_mode: false
log_every_n_steps: 20
check_val_every_n_epoch: 1
deterministic: false
